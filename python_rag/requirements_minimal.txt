# Minimal CPU-only requirements for RAG chatbot
# No NVIDIA packages, no heavy dependencies

# Core API
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
python-dotenv==1.0.0

# Vector Store (CPU-only)
faiss-cpu==1.7.4
numpy==1.24.3

# Embeddings (CPU-only, lightweight)
sentence-transformers==2.3.1

# PDF Processing
PyPDF2==3.0.1

# Google Gemini (for chat)
google-generativeai==0.3.2

# Utils
tqdm==4.66.1
