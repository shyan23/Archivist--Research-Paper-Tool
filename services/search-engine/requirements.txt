fastapi==0.115.6
uvicorn[standard]==0.34.0
arxiv==2.1.3
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
pydantic==2.10.6
python-multipart==0.0.20
aiohttp==3.11.11
aiofiles==24.1.0

# Redis for caching
redis==5.0.1

# Fuzzy string matching
rapidfuzz==3.10.1

# Gemini API for embeddings
google-generativeai>=0.3.0

# Vector database (optional - for future semantic search)
qdrant-client==1.11.3

# These can be installed later when needed for full semantic search
# sentence-transformers (use Gemini embeddings instead)
# torch (not needed with Gemini)
